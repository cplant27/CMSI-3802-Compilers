# print 5 to the "hello";         # make better error, like say expects number or variable

#make 5 with 5;
#add 5 to 5;                  # confusing error, expects not a numeral because it expects a variable id which '5' is invalid for


#make x with 5;
#subtract 5 from x;            #make better error for mismatch types, grammar catches it before analyzer

# LISTS AND BOOLS DONT WORK
# MAKE PRINTS A LIST OF INPUTS TO GET AROUND CONCATENATION
# PRINT STMT DOESNT WORK WITH 0
# possibly add lists can only be one type

make x with 5;
if x is 5 {
  print "X IS 5";
}
ifnot {
  print "X IS NOT 5";
}